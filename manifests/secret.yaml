# NOTE: ${DATABASE_PASSWORD} must be substituted by your CI/CD pipeline
# (e.g. envsubst, Helm, Kustomize) before applying this manifest.
apiVersion: v1
kind: Secret
metadata:
  name: web-app-secrets
  namespace: production
type: Opaque
stringData:
  database_password: ${DATABASE_PASSWORD}

